'use strict';

const depth = require('../lib/depth-first');
const Breadth = require('../lib/breadth-first');

describe('Depth', () => {
  it('can start from different points', () => {
    let graph = new Breadth();
    
  })
})